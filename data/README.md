# Data directory

External inputs are intentionally excluded from Git. Store local data here using subdirectories that match the relevant workflow. Only small, de-identified, redistribution-safe examples belong in `examples/`.

Expected historical inputs include MAT simulation databases, tabular optical-property measurements, voxel/mesh models, and diffuse-reflectance measurements.
